# Only needed if you deploy the frontend separately from the backend
# (different origin). In local dev, the Vite proxy in vite.config.js
# handles this instead, so this can stay unset.
VITE_API_URL=
